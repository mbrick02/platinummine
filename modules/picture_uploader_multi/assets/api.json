{
  "Upload": {
    "url_segments": "picture_uploader_multi/upload/{target_module}/{update_id}",
    "request_type": "POST",
    "description": "The endpoint for gallery uploads.",
    "enableParams": false,
    "required_fields": [
      {
        "name": "update_id",
        "label": "Update ID"
      },
      {
        "name": "target_module",
        "label": "Target Module"
      }
    ],
    "authorization":{  
        "roles": [
            "admin"
        ]
    }
  },
  "Remove": {
    "url_segments": "picture_uploader_multi/upload/{target_module}/{update_id}",
    "request_type": "DELETE",
    "description": "Removes then fetches remaining pictures.",
    "enableParams": false,
    "required_fields": [
      {
        "name": "update_id",
        "label": "Update ID"
      },
      {
        "name": "target_module",
        "label": "Target Module"
      }
    ],
    "authorization":"*"
  }
}